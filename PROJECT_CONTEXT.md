# 発注ツール共有コンテキスト

最終更新: 2026-08-23

このファイルは、発注ツールの設計・実装・確認を行う全員が最初に確認する共有資料です。
会話だけに依存せず、確定した仕様・未確認事項・禁止事項をここに残します。

## 1. プロジェクトの目的

店舗の発注作業を、商品・ジャンル・便・廃棄期限・実績を整理しながら支援する。

優先順位は次のとおりです。

1. 入力したデータを失わない
2. 便・日付・商品を取り違えない
3. ユーザーが確認できる形で計算根拠を表示する
4. 二重入力を減らす
5. 既存の発注フローを壊さない

## 2. 現在のプロジェクト構成

現時点の主なファイル:

- `index.html`: 画面構成
- `app.js`: 画面処理、保存、計算、写真読み取り補助
- `data.js`: 初期データ、商品・ジャンル・実績のデータ
- `style.css`: 画面スタイル

DBサーバーを使う構成ではなく、現状はブラウザ側のデータとクラウド同期を中心に動作している。DBと呼ぶ場合は、実際の保存先・キー・同期処理をコードで確認してから定義すること。

## 3. 現在確認できている機能

- ジャンル別の商品発注画面
- 1便・2便・3便の発注入力
- 発注数の提案と実際の発注の切り替え
- 商品の並べ替え
- 廃棄期限・発注便などの商品設定
- 実績記録画面
- 日付の前後移動
- 日付ごとの入力途中データ保持
- 画面移動・バックグラウンド移行時の保存処理
- クラウド同期
- 天気情報取得
- 写真からストコン実績を読み取るためのAIプロンプト
- メモの保存と翌年表示
- 便別の納品・販売・廃棄入力から、総数を自動計算する処理

## 4. 確定している入力方針

実績記録では、次の扱いにする。

- AI発注数: 表示専用。ユーザーは直接編集しない
- 販売総数: 表示専用。便別販売数から自動計算する
- 廃棄総数: 表示専用。便別廃棄数から自動計算する
- 納品総数: 表示専用。便別納品数から自動計算する
- 便別の納品・販売・廃棄: 必要に応じてユーザーが入力する

写真から読み取った値は、確定前にユーザーが確認できる状態にする。読み取り結果を根拠不明のまま自動確定しない。

## 5. 便の扱い

便はジャンルごとに異なる。全ジャンルを1便・2便・3便で固定して扱わない。

現時点で会話から確認されている例:

- チルド弁当: 3便のみの場合がある
- 冷やし麺: 2便・3便のみ
- ほっと麺: 2便のみの場合がある
- パスタ: 商品により1便・3便、または2便のみ
- 焼き麺: 2便のみの場合がある

画面に入力された数字が「発注数」「総数」「現在庫」のどれか不明な場合、数量判断や自動計算を行わない。

## 6. 写真データの扱い

ストコン画面は、画面ごとに取得できる情報が違う。

### 売上ランキング

取得候補:

- 商品名
- 商品コード（品番）
- 売価
- 大分類・中分類
- 店舗販売数
- 表示されている場合の廃棄数

取得できない、またはこの画面だけでは確定できないもの:

- 納品数
- 便別納品数
- AI発注数
- 便別販売・廃棄
- 粗利額

### 品揃え状況確認・修正

商品名、売価、値入率、週別販売・廃棄、AI推奨値、便別予定数などが表示される場合がある。ただし、写真の解像度と表示状態によって読み取れない項目がある。

### 日別時系列推移表・グラフ

日付、天候、気温、納品、販売、廃棄、前年実績、AI推奨、粗利などが表示される場合がある。AI発注期間かどうかで表示項目が変わる。

### 中分類総数

直近日別、前週日別、前年日別、4週同曜日、4週週別平均などの比較ができる。ただし、画面上の各数字がどの列・便に属するかを確認してから使う。

## 7. 数量判断の必須確認項目

数量を提案する前に、最低限次を確認する。

- 対象日
- 現在時刻
- 今日これから納品される便と数量
- 今日これから売れる予想数
- 現在庫
- 明日の納品可能な便と納品時刻
- すでに入力されている発注数
- 発注数・総数・在庫の定義
- 商品別または中分類別の販売実績
- 天気予報の対象地域と対象日

不足項目がある場合は、まず不足項目だけを質問する。推測で「適正」「これでよい」と断定しない。

## 8. 計算結果の表示ルール

計算を行う場合は、次の順で示す。

1. 入力として使った確定値
2. 入力値の出典画面・日付・便
3. 計算式
4. 計算結果
5. 誤差・未確認事項

例:

```text
明日2便前の予想残数
= 現在庫 + 今日残りの納品数 - 今日残りの販売予測

明日2便の発注数
= 明日2便までに必要な数量 - 明日2便前の予想残数
```

## 9. 実装前の確認ルール

次の順番を必ず守る。

1. 現在のコード・保存形式・画面を確認
2. 確定情報と未確認情報を分ける
3. 不明点を質問する
4. 実装案と影響範囲を提示する
5. ユーザーの了承後に変更する
6. 変更後に構文確認・動作確認・差分確認を行う
7. 必要な場合のみコミット・プッシュする

「既存コードに似た処理がないか」を先に検索する。DB構造・保存先・同期処理を確認せずに新しい保存形式を作らない。

## 10. モデルの使い分け方針

モデル名・提供状況・料金は変わるため、作業開始時に現在のCodex設定を確認する。モデル名だけで品質を断定せず、代表的な作業で確認する。

暫定的な使い分け:

- 軽い確認・ファイル検索・小さな修正: Luna相当
- 通常の実装・テスト・画面修正: Terra相当
- DB設計、保存・同期、複雑な不具合、要件整理: Sol相当または高い推論設定
- 重要な設計判断: 速さより検証を優先し、別モデルまたは再確認を行う

Lunaを使う場合でも、曖昧なデータのまま実装・数量判断を許可しない。モデルを上げても、画面の数字が読めない・項目の意味が不明という問題は解決しないため、追加確認が必要である。

## 11. 作業中の応答ルール

- わからないことは「わからない」と明示する
- 読み取れない数字を補完しない
- 推測は推測として表示し、確定値と分ける
- 変更前に対象ファイルと変更内容を説明する
- 60秒を超えて調査する場合は、進捗を伝える
- 実装した場合は、変更ファイル・確認結果・コミットを報告する
- ユーザーが「まだ実装しない」と指定した場合は、調査と提案だけを行う

## 12. 未確認・今後整理する事項

- 実際のクラウド保存先と同期の競合ルール
- `DB`オブジェクトの正式なスキーマ
- 商品・ジャンル・便・廃棄期限の正式な関係
- 実績記録の各フィールドの正式な意味
- 写真読み取り結果の確定フロー
- AI発注期間終了後に取得できるデータ範囲
- 販売予測・納品目安・廃棄予算を導入するかどうか
- 予測を導入する場合の入力データと検証方法

この章の項目は、確認なしに実装しない。

