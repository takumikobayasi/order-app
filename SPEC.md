# 発注アプリ 仕様書

最終更新: 2026-08-24

このファイルは、他のAI・他の開発者に現状のアプリの動きを伝えるための仕様書です。
「なぜこうしたか」の経緯は含めず、現時点で実装されている仕様だけを書きます。
運用ルールや作業手順は `PROJECT_CONTEXT.md` を参照してください。

対象ユーザー: ファミリーマート加盟店の発注担当者（店長本人）。
店舗独自のWebアプリで、GitHub Pages (`https://takumikobayasi.github.io/order-app/`) で公開し、
店の他のスタッフとも共有して使う想定。

## 1. 全体構成

- ビルド不要のバニラJS/HTML/CSS。フレームワークなし。
- ファイル構成:
  - `index.html` … 画面のDOM構造（ダイアログ含む）
  - `app.js` … 全ロジック（描画・保存・計算・同期・写真読取補助）
  - `data.js` … `GEN`（ジャンル一覧）、`DOW0`/`RAT0`（曜日係数・便構成比の初期値）、`SEED`（初期投入データ）
  - `style.css` … スタイル。狭い画面（560px以下）ではカード内の商品表をグリッドレイアウトに切り替える
  - `js/bootstrap.js` … ES Moduleの初期化と、移行期間中の既存`app.js`起動
  - `js/modules/storage.js` … v3 DBの読込・検証・保存・バックアップ入出力
- キャッシュ対策として `index.html` は `style.css?v=YYYYMMDDHHMM` のようにクエリ文字列でバージョンを持つ。ファイルを更新したら必ずこの数字を更新する。
- データはブラウザの `localStorage`（キー `hacchu.db.v3`、値はJSONをbase64化したもの）に保存し、Google Apps Script (GAS) 経由でクラウド同期する。DBサーバーは無い。
- 保存時は直前の正常なDBを `hacchu.db.v3.backup` に退避する。主DBが破損している場合は `hacchu.db.v3.corrupt` に保全し、正常な直前バックアップがあればそこから起動する。

## 2. データモデル

トップレベルの `DB` オブジェクト:

```js
DB = {
  v: 3,
  active: 'onigiri',        // 現在選択中のジャンルキー
  memoDisplay: true,
  pendingSync: false,       // true=端末にのみ保存された未同期の変更がある
  ts: <number>,             // 最終更新時刻(ms)
  syncedTs: <number>,       // 最後にクラウドと同期できた時刻(ms)
  g: { onigiri: {...}, sushi: {...}, ... }  // ジャンルごとのデータ（下記）
}
```

ジャンル1件分（`DB.g[key]`）:

```js
{
  name: 'おむすび', icon: '🍙',
  items: [ {id, name, price, day, unit, grade, my, tag, memo, margin, pos, posWeek}, ... ],
  hist: [ {d, y, w, ai, n, s, ha, nb, sb, hab, m}, ... ],   // 実績記録（日別）
  tgt: { '8/22': {q, a}, ... },                              // 本部目標（日付キー、個数と金額）
  dow: {月:0.98, 火:1.05, ...},                               // 曜日係数（初期値。自店実績が7日以上あれば学習値で上書き表示）
  rat: { weekday:[59,24,17], sat:[65,35,0], sun:[59,30,11] }, // 便構成比%（曜日タイプ別）
  base: 115, up: 1.1,                                        // 週平均販売数、上乗せ倍率
  shelf: 1, cycle: 1, binNote: '...',                        // 消費期限(日)、発注サイクル(日)、便構成メモ
  cur: { dt: '8/22', v: {}, ai: [...], aiVer: '7:30版', wthr: '朝晴/昼曇/夕雨',
         carry: 71, plan: 38, carryBin: [14,null,null], stockItems: 37, stockSrc: '中分類総数（手づくりデザート） 8/25' },
  binmx: { deliv: [{d,t:[]}×3], waste: [{d,t:[]}×3], rel: true }, // 便と廃棄の時間マスタ（3章参照）
  weekHist: {...}, dowActual: {...},                          // 施策資料などから読み取った参考データ（スナップショット的）
  snap: [ {week, type, at, ...} ]                             // 週次取り込みの蓄積データ（4章参照）
}
```

商品1件（`items[]`の要素）:

```js
{
  id: 'o04',
  name: '直巻 和風ツナマヨネーズ',
  price: 172,          // 売価
  day: 7.3,             // 日販（1日あたりの平常販売数の目安）
  unit: 1,              // 発注単位（2なら2個単位でしか発注できない）
  grade: '◎',           // ◎○△×新 のランク表示
  my: 9,                // 本部目安数
  tag: '', memo: '',    // 任意タグ・メモ
  margin: 40.3,         // 値入率(%)
  pos: { sales:[...4週], waste:[...4週], ai:[1便,2便,3便] },   // POS「品揃え状況確認・修正」の週販売数・週廃棄数・AI推奨
  posWeek: {...}        // 便別の週次実績（一部商品のみ）
}
```

その日の入力値（`cur.v[商品id]`）:

```js
{
  o: [1便, 2便, 3便],   // 「提案」タブの値
  i: [1便, 2便, 3便],   // 「実際の発注」タブの値
  a: [1便, 2便, 3便]    // 予備（現状の画面には出ていない）
}
```

値は数量（個数）。`null`=未入力。便のインデックスは常に「実際の便番号-1」（2便のみのジャンルでも `v[1]` に入る。詰めて `v[0]` には入れない）。

## 3. ジャンル（GEN）と便・廃棄時間マスタ

ジャンルで使う便は次の順で決まる（`itemBins()` / `binsUsed()`）。

1. 商品の `orderBins`（品揃えマスターの「発注便」チェック）
2. 便と廃棄の時間マスタ `binmx`（廃棄時刻が登録されている便）
3. ジャンルの `bins`（そのジャンルで使う便の既定値。例: デザート`[0,1]`＝1便・2便、冷やし麺`[1,2]`＝2便・3便）
4. どれも無ければ1〜3便すべて

`bins` と `binNote` の既定値は `GEN_BIN_DEFAULTS`（`app.js`）に持ち、
`applyGenreBinDefaults()` が未設定のジャンルにだけ入れる（ユーザーが設定した値は上書きしない）。
品揃えマスターの「発注便」チェックは、そのジャンルで使う便と、その商品にすでに入っている便だけを表示する。

`GEN`（`data.js`）は14ジャンル固定: おむすび・寿司・弁当・チルド弁当・サンドイッチ・バラエティーロール・パン・パスタ・冷やし麺・ホット麺・焼麺・サラダ・惣菜・その他。

各ジャンルは1便・2便・3便のうち、実際に使う便が異なる（例: サンドイッチは1便・3便のみ）。
これを設定画面の「便と廃棄の時間（ジャンル別）」で管理する（`binmx`）。

- **納品**は発注日を基準に「発注日／翌日／2日後／3日後」のいずれかを選ぶ（ほぼ「翌日」固定）。
- **廃棄**は納品日を基準に「納品当日／1日後／…／5日後」のいずれかを選ぶ。
- 廃棄の時刻は `2/5/11/14/18/21/24時` から複数選択可（廃棄が1日に複数回に分かれる商品用）。
- 未設定の便（`d`も`t`も空）はそのジャンルで「使わない便」として扱われ、商品別入力の列・配分計算から外れる。
- どの便も未設定のジャンルは、後方互換のため従来どおり1〜3便すべてを表示する。
- 「店に並ぶ時間」（納品〜廃棄までの時間）を`binSpan()`で自動計算して要約表示する。
- 旧バージョン（廃棄日も発注日基準で数えていた頃）のデータは、初回アクセス時に`binmx.rel`フラグの有無で判定し、納品日基準に自動変換する。

## 4. 画面構成

### ヘッダー
2段構成。上段は画面タブ（📋発注／💰利益・廃棄／📷カメラ取り込み、`renderTabs()`）、
下段はジャンルタブ（横スクロール、品数表示、`renderGenreTabs()`）。設定ボタンとテーマ切替も上段に置く。

- ジャンルタブは `GEN` の全ジャンルを並べ、押すと `switchGenre()` で `DB.active` を切り替える。
- どの画面もジャンルごとに中身が変わるため、ジャンルタブは常に表示する。
- 切替時は、切替前と切替先の両方で `saveOrderDateDraft()` を呼んでから移動するため、入力途中の発注は日付ごとに残る。
- 納品日と天気は切替先へ引き継ぐ（同じ納品日のまま次のジャンルを発注できるようにするため）。
  切替先にその日付の下書きがあれば `loadOrderDate()` で復元する。
- 選択中のジャンルが画面外に出ないよう、行だけを横スクロールさせる。

### カード0: セットアップ（`renderSetup()`）
納品日・天気（🌤自動ボタンでOpen-Meteoから取得）・ストコンAI1便/2便/3便の総数入力・配信版（7:30版/13:00版/18時版）・前日実績記録ボタン。
下に自動生成される確認事項リスト（下記5章）。

### カード1: 目標から自動配分
目標個数・目標金額を入力し「配分」ボタンで商品別に按分する（6章）。

### カード2: 商品別入力（`renderItems()`）
- タブ「**提案**」/「**実際の発注**」（`MODE`='o'/'i'）を切り替えて同じ商品表に別の値を入力する。
- 「提案」＝アプリが計算する参考値。締切ロックの対象外で、いつでも編集・再計算できる。
- 「実際の発注」＝本当に発注した数。締切ロック（1便は納品日10:00、2便/3便は15:00）の対象。ロック中は`toggleLockOverride()`で一時解除できる。
- 列はジャンルのマスタ設定に応じて、使う便だけを表示する。列見出し・入力欄の上に廃棄時刻を添える（例「1便 5時廃棄」）。
- 「ストコンAIをコピー」で「実際の発注」の値を現在のタブにコピーできる。
- 表全体を作り直すとスマホのキーボードが閉じてしまうため、入力確定時は行の計・合計行・単位警告だけを差分更新する（`refreshSum()`）。
- 並べ替えモードON/OFFで商品の並び替えUIが出る。

### 利益・廃棄タブ（`renderProfit()`）
画面上部のタブ「💰 利益・廃棄」。いま入っている発注数に対して、粗利予想と廃棄ロス予想を比べる**表示専用**の画面。
発注数の書き換えは一切行わない（読み取りのみ）。新しい入力欄は増やさず、既存データだけを使う。

- 使う数字: `item.price`（売価）、`item.margin`（値入率。未登録ならジャンルの`g.margin`を使い、出典を画面に出す）、
  `item.day`（日販、無ければ `item.rank` の販売÷日数、無ければ `item.pos.sales` の直近週÷7）、
  `item.pos.sales`/`item.pos.waste`（実績廃棄率）、`item.unit`（発注単位）、`item.my`（本部目安）、
  発注数は「実際の発注」が入っていればそれ、無ければ「提案」（商品ごとに判定し、出典を画面に出す）。
- 計算式:
  - 売り切るまでに使える日数 ＝ 期限日数と発注サイクル日数の長い方（`itemShelfDays()`）。
    期限日数は 商品の`shelf` → 商品の廃棄区分`dclass`（D3なら3日、D0は1日） → ジャンルの`shelf` の順で決め、
    どれも無ければ1日として扱い、その旨を画面に出す。
  - 需要見込み ＝ 日販 × 曜日係数（`dowInfo()`）× 天気係数（`weatherFactor()`）× 売り切るまでに使える日数
  - 予想廃棄数 ＝ 「発注数 − 需要見込み（期限内に売り切れない分）」と「発注数 × 実績廃棄率」の大きい方（発注数が上限）
  - 予想販売数 ＝ 発注数 − 予想廃棄数
  - 粗利予想 ＝ 予想販売数 ×（税込売価 − 原価）
  - 廃棄ロス予想 ＝ 予想廃棄数 × 原価　※廃棄は原価ロスとして数える
  - 原価は `item.cost` があればその値、無ければ 税込売価 ×（1 − 値入率）。
    **ストコンの値入率は税込売価ベース**（例：売価298円・原価186.07円で値入率37.5%）なので、
    税抜売価に値入率を掛けてはいけない。税込売価は `item.priceIn`（写真から読んだ値。税抜に丸めた値と
    食い違う場合は使わない）、無ければ `price × 1.08`。
- 「抑え目安」は、本部目安（`my`）と発注単位（`unit`）を下回らない範囲で発注数を1単位ずつ減らし、差引がいちばん大きくなる数。
  現在の発注数より少なくなるときだけ表示し、改善額を併記する。
- ジャンル合計で廃棄ロス予想が粗利予想を上回ると赤字判定を表示する。`weekHist.amount.cur.weekAvg` があれば
  実績の週平均（粗利・廃棄原価）を出典付きで参考表示する（計算には使わない）。
- 売価・値入率・日販のいずれかが未登録の商品は推測せず、「データ不足で計算できない商品」として名前と不足項目を並べる。
- 現在庫（`cur.carry`）が入っている場合、ジャンル合計に「在庫込みの見通し」を出す。
  現在庫＋納品予定＋発注合計 と、各商品の期限日数ぶんの需要見込み合計を比べ、余る見込みの個数と原価換算額を表示する。
  在庫がどの商品にあるかは分からないため、商品ごとの計算には使わない。
  `cur.stockItems`（ストコンのアイテム数）がアプリの登録商品数より多い場合は、網羅していない参考値である旨を出す。
- 需要見込みは推測値のため、機会ロス（欠品による売り逃し）を含めていない旨を画面下部に明記する。

### まとめて入力ダイアログ（`dBulk`）
商品別入力カードの「まとめて入力」ボタンで開く。1つの項目だけを商品名と縦に並べて入力する画面。
品揃えマスターは列が多く横スクロールが必要なため、スマホで1項目を入れ切るための画面として用意している。

- 項目は 値入率／日販／売価(税抜)／廃棄D／本部目安 を切り替え。切り替え時に入力済みの値を保存してから移る。
- 「空欄にまとめて入れる」「全部に入れる」で同じ値をまとめて入れられる。
- 入力済みの行は色を変え、下部に「◯/◯品 入力済み」を出す。
- 保存すると商品データに反映し、発注表・セットアップ・利益廃棄を描き直す。

### カード3: 週次データ取り込み（取り込み先はジャンルタブで選んでいるジャンル）
月曜/火曜の写真取り込み手順とAIプロンプトのコピー、貼り付け欄（8章参照）。

### 実績記録ダイアログ（`dHist`）
日付を◀▶で移動（デフォルトは前日から開始）。
- 天候は日付に応じて自動取得（過去日は確定値、未来日は予報。10章参照）。
- 便別の納品数は「実際の発注」の値から自動入力（`sums('i')`）。
- 便別の販売・廃棄数はユーザーが手入力。廃棄欄のラベルは実際の便別廃棄時刻（1便=5時／2便=14時／3便=21時、ストコン画面の表示に合わせた固定値）。
- AI発注数・販売総数・廃棄総数・納品総数は表示専用で、便別の値から自動計算する。
- メモ欄（施策・イベント記録）。去年の同じ週・同じ月のメモを自動表示する。

### 設定ダイアログ（`dSet`）
GAS同期URL、天気の地域設定、需要の基準（週平均販売数・上乗せ倍率・消費期限・発注サイクル・便構成メモ）、便と廃棄の時間マスタ（3章）、目標値登録、履歴テーブル、メモ一覧（週/月切替）、書き出し・AI連携（8章）、写真読取（9章）、全部消す（危険操作、確認ダイアログ2段階）。

## 5. セットアップカードの自動確認表示

`renderSetup()`が毎回、次のような行を生成する（不明な項目は`warn`色で表示）:

- 納品日・曜日係数・便構成比（学習済みなら「自店n日から学習」と表示）
- 特殊カレンダー期間（GW/お盆/年末年始）は「ストコンAIの学習対象外」の注意を追加
- 締切状況（1便/2便3便のロック有無）
- ストコンAI配信版（現在の版と次回更新時刻。`aiVersionNow()`）
- 提案の採用率（「提案」と「実際の発注」の一致率。※7章参照）
- 本部目標、今日の需要見込み（天気・曜日から算出した提案数、`demand()`）
- 提案数とストコンAI総数の差（15%以上ずれたら警告）
- 数日おき発注カテゴリ向け: 期限内に売り切れる上限との比較、在庫のもち日数の判定
- 提案中（「提案」タブの現在の合計）

## 6. 需要予測・自動配分ロジック

- `weatherFactor(天気文字列, 便構成比)`: 「朝◯/昼◯/夕◯」形式の天気を、便構成比で加重した係数にする（晴1.05/曇0.97/雨0.90/雪0.82）。
- `demand()`: `週平均販売数 × 曜日係数 × 上乗せ倍率 × 天気係数` で当日の見込み数を算出。チルド弁当は月曜のみ倍率1.5倍の特例あり。
- `dowInfo()`: 曜日係数は自店の実績が7日分以上蓄積されると`learnDow()`による学習値に切り替わる（4日以上の曜日でデータがあれば採用）。
- `alloc()`（「配分」ボタン、提案タブへ書き込む）:
  1. 商品ごとに「実際の発注」に便別の数字が入っていれば、その数をそのまま「提案」にコピーする（計算しない）。
  2. 残りの商品だけを対象に、目標個数/金額（未入力なら`demand()`の値、要確認ダイアログ表示）から「実際の発注」入力済み分を差し引いた残り目標を按分する。
  3. 按分は「廃棄実績×値入率で調整した重み(`edayOf()`)の上位50%に傾斜配分」というファミマ発注マニュアル準拠のロジック（上位50%品目に残り数量を重み比例配分、下位はほぼ日販どおり）。
  4. 便への振り分け比率は、設定の「ストコンAI1便/2便/3便」総数から実際の発注入力済み分を引いた残りがあればそれを優先し、無ければ便構成比%（`r1/r2/r3`、曜日タイプ別の初期値または手動上書き）を使う。
  5. 各アイテムの最終値は`unit`（発注単位）に丸められる（`fix2()`）。

## 7. 「提案」と「実際の発注」の関係

- `sums('o')`＝提案の合計、`sums('i')`＝実際の発注の合計。両方とも「他」列で比較表示できる。
- `getAiAdoption()`: 「実際の発注」に値がある商品のうち、「提案」と完全一致する商品の割合。70%未満は「一部を調整して発注」、40%未満は「提案から大きく調整して発注」と表示（善悪の判定ではなく状態の説明）。
- 締切ロックは「実際の発注」タブに適用される（1便=納品日10:00、2便/3便=15:00）。「提案」タブは常に編集・再計算できる。
- 実績記録の便別納品オート入力は「実際の発注」の値を使う。

## 8. AI連携（コピペ運用）

このアプリはAPIに直接つながず、テキストのコピペで外部のAIチャット（利用者が使い慣れたもの）とやり取りする設計。

- 「AI分析用プロンプト」（`outPrompt()`）: 対象日・曜日係数・特殊カレンダー・提案の採用率・本部目標・需要見込み・ストコンAI推奨合計・提案合計・直近実績推移・提案からの変更商品一覧をまとめたテキストを生成し、発注バランスの評価・改善提案を依頼する文面にする。
- 「AIに送る（短い）」（`outCompact()`）: 上記を圧縮した1行〜数行版。
- 「バックアップ」（`outFull()`）: `#BK3:<base64>` 形式でDB全体をテキスト化（他端末への手動移行・退避用）。
- 「読み込み」（`doImport()`）: `#BK3:...` を貼り付けて復元。
- 週次取り込み（`copyWeeklyPrompt()`/`importWeekly()`）: 月曜は「日別時系列推移グラフ」の写真、火曜は「品揃え状況確認・修正」の写真をAIチャットに送り、決まった形式（`#UPD1:{...}`）で返してもらってから貼り付ける。
  - 月曜形式: `{"type":"mon","days":[{"d":"8/17","ai":134,"n":134,"s":126,"ha":14}]}` → `hist`に反映。
  - 中分類総数形式: `{"type":"mid","gen":"手づくりデザート","d":"8/25","stock":71,"plan":38,"carry":[14,null,null],"sub":[{"name":"ケーキ","stock":34,"plan":7,"items":[7,12,0]}]}`
    → いま開いている発注日の `cur.carry`（現在庫）・`cur.plan`（納品予定）・`cur.carryBin`（便別繰越）・`cur.stockItems`（小分類のアイテム数合計）に反映する。
    小分類の現在庫・納品予定の合計が中分類の値と合うか検算し、合わない場合は読み取り違いとして警告を出す。
  - 新商品案内明細形式: `{"type":"new","d":"8/24","items":[{"name":...,"price":198,"cost":125.25,"margin":36.7,"code":"0410113","unit":1,"start":"08/24"}]}`
    → 売価（税込。税抜に直して`price`、税込は`priceIn`）・`cost`（原価）・`margin`（値入率）・`code`・`unit` を反映する。
    売価と原価から値入率を計算し直し、写真の値入率と0.5ポイント以上ずれる行は警告を出して計算値のほうを採用する。
  - 売上ランキング形式: `{"type":"rank","gen":...,"from":"8/24","upd":"8/25","items":[{"name":...,"code":...,"price":334,"s":7,"w":null}]}`
    → 商品を登録（未登録は追加）し、売価（画面は税込表示のため`/1.08`して税抜で保存。すでに売価がある商品は上書きしない）、
    商品コード、`item.rank`（対象期間の販売・廃棄と日数）を入れる。日販が未登録の商品は「販売数÷対象日数」で仮入力する。
    この画面には値入率・納品数が無いため触らない。対象日数は `rankDays()` が開始日と更新日から数える（1〜7日）。
  - 火曜形式: `{"type":"tue","items":[{"name":...,"price":...,"margin":...,"ai":[...],"ws":[...],"ww":[...],"new":bool}]}` → 商品ごとの週販売数・週廃棄数を`snap`に蓄積し、`itemTrend()`で前週比を計算・表示する。
  - 取り込み先はジャンルタブで選んでいるジャンル。取り違え防止のため、貼り付け欄の上に取り込み先ジャンル名と品数を常時表示する。
  - 「未登録の商品もこのジャンルに追加する」チェック（`#wkAddNew`）: 既定は商品が0件のジャンルのときだけON。
    ONのとき、写真にあって未登録の商品を`new`でなくても追加する（商品未登録のジャンルを写真から作るため）。
    ユーザーが操作した後は自動でON/OFFしない。
  - 追加時、週販売数（`ws`）があれば日販を「直近週÷7」で仮入力する（実績から計算した値。メモに仮値である旨を残し、取り込み結果にも件数を出す）。
    `new`の商品は適性「新」・印「新」、それ以外は適性「○」・印なしで登録する。
- 「📷 写真読取」（`handleImage()`）: Tesseract.jsでOCRを試みるが、POSの表形式画面では精度が低く実運用では上記コピペ運用が主。

## 9. 天気の自動取得

- Open-Meteo（APIキー不要）を使用。地域は緯度経度で保持（設定でジオコーディング検索して保存）。
- `weatherForDate(日付)`: 過去日は確定値、未来日は予報。予報APIで取れない過去日（92日より前）は`archive-api.open-meteo.com`にフォールバックする。
- 取得した過去日の天気はローカル（`hacchu.wx.v1`）にキャッシュし、同じ日付への再取得は通信しない。
- 実績記録の日付を移動するたびに`autoHistWeather()`が自動取得し、既存の手入力は上書きしない（差異がある場合のみ注記表示、🌤ボタンで上書き可能）。
- セットアップカードの🌤ボタンも同じ`weatherForDate()`を使う。

## 10. クラウド同期

- Google Apps Script (GAS) のWebアプリURLを設定に保存（複数端末で共通のURLを使う）。
- 保存（`syncCloud()`）: `mode:'no-cors'`でPOST。空データでの上書き事故を防ぐため、商品0件かつ履歴0件のときは確認ダイアログを出す。
- 起動時（`cloudLoadSilent()`）: JSONPで読み込み、クラウドの`ts`が自端末の`syncedTs`と異なれば自動反映。ただし「クラウドが空」「端末に未同期の変更がある」場合は上書きしない。
- 手動読込（`cloudLoad()`）: JSONP、15秒タイムアウト。読込後は設定ダイアログを閉じて件数を通知する。
- 読込時は必ず`ensureCategories()`でGENの全14ジャンルを補完してから反映する（新ジャンル追加時に既存データが欠けないようにするため）。

## 11. 締切ロック・特殊カレンダー

- `lockState()`: 1便は納品日10:00、2便/3便は15:00でロック（ストコンへの送信済み想定）。`toggleLockOverride()`で一時解除可能。
- `specialPeriod()`: GW・お盆・年末年始を検出し、「ストコンAIの学習対象外のため意思入れ必須」の注意を出す。
- `aiVersionNow()`: 現在時刻から、ストコンAIの配信版（7:30版/13:00版/18時版）と次回更新時刻を判定する。

## 12. 用語集

| 用語 | 意味 |
|---|---|
| 便 | 商品の納品タイミング。1便・2便・3便。ジャンルごとに使う便が異なる |
| ストコンAI | ファミリーマート本部の発注推奨システム（本アプリの外部） |
| 提案 | 本アプリが計算する参考発注数（`v.o`）。締切ロックなし |
| 実際の発注 | 本当に発注した数（`v.i`）。締切ロックあり、実績記録の便別納品にも使われる |
| 本部目標 | ストコンAIまたは施策資料から得られる、日付ごとの目標個数・金額（`tgt`） |
| 日販 | 商品1点あたりの1日平均販売数の目安（`item.day`） |
| 値入率 | 商品の粗利率（`item.margin`、%） |
| 便構成比 | 1便・2便・3便への配分比率（`rat`、曜日タイプ別） |
| 消費期限／発注サイクル | 数日おきにまとめ発注するジャンル向けの在庫もち計算に使う値（`shelf`/`cycle`） |
| 特殊カレンダー | GW・お盆・年末年始など、AIの学習対象外になる期間 |

## 13. 未確認・今後整理する事項

`PROJECT_CONTEXT.md` の「12. 未確認・今後整理する事項」も参照。特に以下は仕様として未確定:

- パンのファミマ／山崎など、1ジャンルに複数納入業者がある場合の便マスタの扱い（ジャンル分割か、行を分けるか、店で検証中）
- 冷やし麺など、実データに基づく`shelf`/`cycle`/`base`の具体値（店での実測待ち）
- `v.a`（予備の値配列）の用途（現状画面には出ていない）
